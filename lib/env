#!/usr/bin/env bash

declare DB="/app/db/ynkr.db"
declare DOWNLOADS="/app/downloads"
declare MUSIC_DIR="/app/music"
declare DEBUG=true

declare -A YNKR_PLAYLIST

YT_ARCHIVE=/app/.cache/archive.yt
[[ -e "$YT_ARCHIVE" ]] || {
  mkdir -p /app/.cache
  touch "$YT_ARCHIVE"
}

SLEEP=69
